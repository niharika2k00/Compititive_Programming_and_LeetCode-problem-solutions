/*    for (i = 0; i < len; i++)
       {
           for (j = i + 1; j < len; j++)
               if (abs(nums[i] - nums[j]) == k || abs(nums[j] - nums[i]) == k)
                   count++;
       } */